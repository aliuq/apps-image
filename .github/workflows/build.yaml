name: Build and Distribute Docker Image

on:
  push:
    branches:
      - master
    paths:
      - 'Dockerfile'
      - 'app/**'
      - 'base/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Build Docker image
        run: |
          # 格式：
          # 1. ghcr.io/aliuq/app-xxx:latest
          # 2. hub.docker.com/linkaliu/app-xxx:latest
          #
          # changed_charts=$(git diff --name-only HEAD^ | grep -E 'charts/[^/]+/Chart.yaml' | sed -E 's|/Chart.yaml||' | sort | uniq)

          changed_names=$(git diff --name-only HEAD^ | grep -E '^(app|base)/' | sort | uniq)
          echo $changed_names

          names=$(git diff --name-only HEAD^ | awk -F/ '{print $1}' | sort -u)
          echo $names

      # - name: Build Docker image
      #   run: docker build -t myapp:latest .

      # - name: Login to Docker Hub
      #   uses: docker/login-action@v1
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Push to Docker Hub
      #   run: docker push myapp:latest

      # - name: Login to GitHub Container Registry
      #   uses: docker/login-action@v1
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.repository_owner }}
      #     password: ${{ secrets.GITHUB_TOKEN }}

      # - name: Tag Docker image for GitHub Container Registry
      #   run: docker tag myapp:latest ghcr.io/${{ github.repository_owner }}/myapp:latest

      # - name: Push to GitHub Container Registry
      #   run: docker push ghcr.io/${{ github.repository_owner }}/myapp:latest
