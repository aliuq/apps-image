{
  "name": "action",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "A tool to check application updates",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "bun build ./src/index.ts --outdir ./dist --target node --format esm --minify --sourcemap=linked --watch",
    "build": "bun build ./src/index.ts --outdir ./dist --target node --format esm --minify --sourcemap=linked"
  },
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.1",
    "@actions/glob": "^0.5.0",
    "@octokit/core": "^7.0.3",
    "@types/fs-extra": "^11.0.4",
    "fast-glob": "^3.3.3",
    "fs-extra": "^11.3.0",
    "kolorist": "^1.8.0",
    "octokit-plugin-create-pull-request": "^6.0.1",
    "typescript": "^5.8.3"
  }
}
